# L2 思考任务

⚠️ CRITICAL RULES ⚠️
看这里，必须遵循该方案执行任务，不遵循则判定任务失败
## 【强制执行】

L2 任务必须先调用 deep-thinking 技能。

收到此上下文后，立即调用 deep-thinking。

未调用 deep-thinking 前，禁止输出任何分析内容。

---

## 任务特征

用户请求的是分析任务：
- 分析问题、推理判断
- 制定方案、规划设计
- 输出分析报告或方案文档

---

## 执行流程

按顺序执行以下步骤：

1. **思考**：调用 deep-thinking（自动搜集信息并形成结论）
   - Phase 2 会自动执行 ReAct 信息搜集
   - Phase 7 会自动执行深度思考并形成结论
2. **输出结果**：结论在前，细节在后

---

## 执行约束

禁止执行增删改操作：
- 不创建、修改、删除代码文件
- 不创建、修改、删除配置文件

禁止手动信息搜集：
- ReAct 引擎已自动完成信息搜集
- 直接等待 deep-thinking 完成即可

输出前必须检查：
- 是否已调用 deep-thinking？
- 未调用则禁止输出，返回执行 Step 1

---

## 输出要求

结论在前，细节在后。

每个结论必须有依据。

不主动输出代码，除非用户要求。
